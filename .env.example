# Supabase Postgres 连接串（Supavisor 事务池，TLS 必开）
# 形如：postgres://postgres.<ref>:<password>@<region>.pooler.supabase.com:5432/postgres
DATABASE_URL=

# 后台 access token 的 HMAC 签名密钥（生成：openssl rand -hex 32）
TOKEN_SIGNING_SECRET=
